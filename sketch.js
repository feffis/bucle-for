function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255, 182, 193);
}
function draw() {
let pastelBlue=color(173,216,230)
fill (pastelBlue)
for(let x=0;x<=width;x+=50)
    for(let y=0;y<height; y+=50)
let size=()


}
